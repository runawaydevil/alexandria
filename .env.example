# Default repository configuration for Alexandria
# These variables determine which repository's README is shown on the home page

# GitHub repository owner (username or organization)
VITE_DEFAULT_OWNER=runawaydevil

# GitHub repository name
VITE_DEFAULT_REPO=alexandria

# Example for a custom repository:
# VITE_DEFAULT_OWNER=myusername
# VITE_DEFAULT_REPO=my-docs-repo